
#pragma once

#include <string>
#include <vector>

int to_int(const std::string &s);
unsigned long to_long(const std::string &s);
std::string trim_space(const std::string& s);
std::vector<std::string> divide_words(const std::string &str);
std::string to_stringgg(int num);
